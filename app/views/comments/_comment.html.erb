<% div_for comment do %>
  <p>
  <%= image_tag User.find(comment.user_id).gravatar_url() %>
    <strong><%= User.find(comment.user_id).username %></strong> sayd <%= time_ago_in_words(comment.created_at) %> ago<br />
    <%= h(comment.body) %>
  </p>
<% end %>
